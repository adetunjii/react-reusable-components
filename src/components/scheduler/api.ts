// fetch list of events for a particular day
// - need date and time, check if is recurring,
// - if is recurring, get list of recurring dates [mon, tue, wed ....]

/**
 * Takes a start date, an end date, and an eventsRef
 */
const fetchEventOccurences = () => {};
